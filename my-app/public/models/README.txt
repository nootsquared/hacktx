Place your car GLB model here. Current homepage references:

  2022 Toyota GR86 3D Model.glb

Notes
- This path is referenced by the homepage component at src/app/components/CorollaModel.jsx.
- Recommended: Unlit or PBR GLB/GLTF around ≤ 15–25 MB for fast loads.
- If your model requires textures, ensure they are embedded in the GLB.
- Auto-rotate is enabled and the model sits below the tagline.

Optional adjustments
- You can tweak rotation speed via the `rotation-per-second` attribute in CorollaModel.jsx.
